# Feature Flags

Experimenting with `flipt` as a feature flag solution.

Flipt v2 does not have a webhook system. Also, the UI is not that good. It is 
objectively better to actually use it in headless mode and only access the Flipt 
API and build your own UI on top of it.
