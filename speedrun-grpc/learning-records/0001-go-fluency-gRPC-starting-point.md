# Go fluency, gRPC starting point

The learner reports more than two years of Go experience and can read Go quickly, but has not worked with gRPC. Future lessons should assume Go fluency and concentrate on the RPC contract, client lifecycle, failure semantics, and repository-specific conventions.
